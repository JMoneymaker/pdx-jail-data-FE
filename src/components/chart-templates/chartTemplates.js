const ageTemplate = {
  xLabel: 'Number of People in Detention', 
  yLabel: 'Age Range',
  chart: 'VBar'
};

const facilityTemplate = {
  chart: 'Pie'
};

const genderTemplate = {
  xLabel:'Number of People in Detention',
  yLabel:'Gender',
  chart: 'BarH'
};

const raceTemplate = {
  xLabel:'Number of People in Detention',
  yLabel:'Race',
  chart: 'VBar'
};

const trendTotalsTemplate = {
  xLabel: 'Number of People in Detention',
  yLabel: 'Date',
  chart: 'Area'
};

export {
  ageTemplate,
  facilityTemplate,
  genderTemplate,
  raceTemplate,
  trendTotalsTemplate
}; 
